using UnityEngine;
using System.Collections;

public class VLCamera : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
        //if( Input.GetKey(KeyCode.D))
        //{
        //    this.transform.Translate( Vector3.right * 0.05f , Space.Self);
        //}
        //if (Input.GetKey(KeyCode.A))
        //{
        //    this.transform.Translate(Vector3.left * 0.05f, Space.Self);
        //}
        //if (Input.GetKey(KeyCode.W))
        //{
        //    this.transform.Translate(Vector3.forward * 0.05f, Space.Self);
        //}
        //if (Input.GetKey(KeyCode.S))
        //{
        //    this.transform.Translate(Vector3.back * 0.05f, Space.Self);
        //}

	}
}
